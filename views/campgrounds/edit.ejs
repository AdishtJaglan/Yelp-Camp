<% layout("layouts/boilerplate") %>

    <h1>Edit Campground</h1>

    <form action="/campground/<%= findCampground._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Enter New Title:</label>
            <input type="text" name="title" id="title" value="<%= findCampground.title %>">
        </div>
        <br>
        <div>
            <label for="location">Enter New Title:</label>
            <input type="text" name="location" id="location" value="<%= findCampground.location %>">
        </div>

        <button>Update!</button>
    </form>

    <a href="/campground">Home</a>